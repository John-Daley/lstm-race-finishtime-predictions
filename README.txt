This is a repository using LSTM's to predict a racers finish time in a ultra marathon.

It is being built using tensorflow, numpy, pandas, matplotlib and other libraries. It will be a summation of 
different tutorials as this is mainly focused as a learning project. 

Data will be created and used for training. 